# Local Development Environment (Docker-based)
# Copy this to .env.local and fill in your values

# ============================================================================
# REQUIRED
# ============================================================================

# NextAuth Secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your-auth-secret-here

# Credential Encryption Key (generate with: openssl rand -base64 32)
# IMPORTANT: Must be different from AUTH_SECRET
ENCRYPTION_KEY=your-encryption-key-here

# PostgreSQL (from docker-compose.yml - mapped to port 5434 on host)
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/b0t_dev

# Redis (from docker-compose.yml)
REDIS_URL=redis://localhost:6380

# ============================================================================
# OPTIONAL
# ============================================================================

# Environment
NODE_ENV=development
LOG_LEVEL=info

# Admin login credentials (change after first login)
ADMIN_EMAIL=admin@b0t.dev
ADMIN_PASSWORD=admin

# ============================================================================
# IMPORTANT NOTE
# ============================================================================
# Platform API keys (OpenAI, Twitter, YouTube, Instagram, etc.) are managed
# by users through the web UI at: Settings → Credentials
#
# Each user manages their own API keys and quotas.
