# ================================
# REQUIRED
# ================================

# PostgreSQL database (stores workflows, users, credentials, execution history)
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/b0t_dev

# Redis queue (powers workflow execution and job scheduling)
REDIS_URL=redis://localhost:6380

# Session encryption key (generate with: openssl rand -base64 32)
AUTH_SECRET=

# Credential encryption key (generate with: openssl rand -base64 32)
# CRITICAL: Losing this means ALL stored credentials become unrecoverable
ENCRYPTION_KEY=

# ================================
# OPTIONAL
# ================================

# Application URL (for OAuth callbacks and public URLs)
NEXTAUTH_URL=http://localhost:3123
NEXT_PUBLIC_APP_URL=http://localhost:3123

# Initial admin account
ADMIN_EMAIL=admin@b0t.dev
ADMIN_PASSWORD=admin

# Environment
NODE_ENV=development
LOG_LEVEL=info

# ================================
# ADVANCED (Optional)
# ================================

# Database connection pool
# DB_POOL_MAX=20
# DB_POOL_MIN=5

# Workflow concurrency (max workflows processed simultaneously)
# WORKFLOW_CONCURRENCY=20
